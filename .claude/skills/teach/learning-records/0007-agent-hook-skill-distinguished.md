# Agent、Hook、Skill 概念已区分

用户正确区分了三个核心概念：/teach 是 skill（收到指令后由 agent 自主决定教学方式），而非 hook（自动触发）。能用自己的语言解释区别。

同时发现本地没有 hook 配置——这意味着后续课程可以从"配置第一个 hook"入手，有真实的动手素材。

**Evidence:** 用户准确回答了三者区别的作业题，并主动检查了本地的 settings.json 确认无 hook 配置。

**Implications:** 概念层已建立。下一步可以深入：agent 内部如何运作（决策循环），或者让用户亲手配置第一个 hook。后者更符合"动手优先"的偏好。
