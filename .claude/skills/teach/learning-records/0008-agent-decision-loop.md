# Agent 决策循环已理解

用户学习了 agent 内部的核心机制——决策循环（Observe → Think → Act）。能用自己的话解释 agent 为什么不只是"一次性回答"而是多轮循环，以及什么因素塑造了每一步的决策。

用户配置的 PostToolUse hook 被重新定位为"观察决策循环的窗口"——从"这是什么 hook"升级到"我能用 hook 看到什么"。

**Evidence:** 完成了 Lesson 8 的测验和理解题；能将 hook 日志中的工具调用序列还原为 agent 的"思考轨迹"。

**Implications:** 用户现在理解了 agent 如何工作，下一步可以学习 agent 的具体能力来源——Tool 和 MCP 协议（agent 的"手"如何接入外部世界）。
